#!/bin/bash

echo "Run generate"
export PATH=$PATH:$(go env GOPATH)/bin

# Check/install grpcio-tools for Python code generation
if ! python3 -c "import grpc_tools" 2>/dev/null; then
    echo "grpcio-tools not found, installing..."
    pip3 install grpcio-tools
fi

make gen